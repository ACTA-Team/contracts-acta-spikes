[package]
name = "registry-core"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }
