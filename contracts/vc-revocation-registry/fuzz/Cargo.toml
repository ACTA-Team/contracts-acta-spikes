[package]
name = "vc-revocation-registry-fuzz"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
soroban-sdk = { version = "27.0.3", features = ["testutils"] }
vc-revocation-registry-contract = { path = "..", features = ["testutils"] }

# Prevent this fuzz crate from appearing in `cargo build` for the workspace
[workspace]

[[bin]]
name = "fuzz_credential_id"
path = "fuzz_targets/fuzz_credential_id.rs"
test = false
doc = false
